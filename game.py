import json\n\nclass Room:\n    def __init__(self, name, description):\n        self.name = name\n        self.description = description\n        self.connections = {}\n        self.items = []\n        self.npcs = []\n        self.locked = False\n        self.lock_item = None\n\n    def connect(self, direction, room):\n        self.connections[direction] = room\n\n    def get_room(self, direction):\n        return self.connections.get(direction)\n\n    def add_item(self, item):\n        self.items.append(item)\n\n    def remove_item(self, item):\n        self.items.remove(item)\n\n    def add_npc(self, npc):\n        self.npcs.append(npc)\n\n    def remove_npc(self, npc):\n        self.npcs.remove(npc)\n\n    def lock(self, item):\n        self.locked = True\n        self.lock_item = item\n\n    def unlock(self, item):\n        if item == self.lock_item:\n            self.locked = False\n            self.lock_item = None\n\nclass Game:\n    def __init__(self):\n        self.rooms = {}\n        self.current_room = None\n\n    def load_story(self, filename):\n        with open(filename, 'r') as file:\n            story = json.load(file)\n            for room_data in story['rooms']:\n                room = Room(room_data['name'], room_data['description'])\n                self.rooms[room.name] = room\n            for room_data in story['rooms']:\n                room = self.rooms[room_data['name']]\n                for direction, connected_room in room_data['connections'].items():\n                    room.connect(direction, self.rooms[connected_room])\n            self.current_room = self.rooms[story['start']]\n\n    def move(self, direction):\n        if self.current_room.locked:\n            return "You can't go that way, the door is locked."\n        next_room = self.current_room.get_room(direction)\n        if next_room:\n            self.current_room = next_room\n            return f'You are now in {next_room.name}.\n{next_room.description}'\n        return "You can't go that way."\n