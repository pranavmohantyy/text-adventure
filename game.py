import json\n\nclass Room:\n    def __init__(self, name, description):\n        self.name = name\n        self.description = description\n        self.connections = {}\n        self.items = []\n        self.npcs = []\n        self.locked = False\n        self.lock_item = None\n\n    def connect(self, direction, room):\n        self.connections[direction] = room\n\n    def get_room(self, direction):\n        return self.connections.get(direction)\n\n    def add_item(self, item):\n        self.items.append(item)\n\n    def remove_item(self, item):\n        self.items.remove(item)\n\n    def add_npc(self, npc):\n        self.npcs.append(npc)\n\n    def describe(self):\n        return f\"{self.name}\n{self.description}\nItems: {', '.join(self.items) if self.items else 'None'}\nNPCs: {', '.join(self.npcs) if self.npcs else 'None'}\"\n\n    def lock(self, item):\n        self.locked = True\n        self.lock_item = item\n\n    def unlock(self, item):\n        if item == self.lock_item:\n            self.locked = False\n            self.lock_item = None\n\nclass Game:\n    def __init__(self):\n        self.rooms = {}\n        self.current_room = None\n        self.inventory = []\n        self.visited_rooms = []\n\n    def add_room(self, room):\n        self.rooms[room.name] = room\n\n    def move(self, direction):\n        if self.current_room.locked:\n            return 'Room is locked.'\n        next_room = self.current_room.get_room(direction)\n        if next_room:\n            self.current_room = next_room\n            self.visited_rooms.append(next_room.name)\n            return self.current_room.describe()\n        return 'You can\'t go that way.'\n\n    def look(self):\n        return self.current_room.describe()\n\n    def inventory_command(self):\n        return f\"Inventory: {', '.join(self.inventory) if self.inventory else 'Empty'}\"\n\n    def map_command(self):\n        return f\"Visited rooms: {', '.join(self.visited_rooms) if self.visited_rooms else 'None'}\"